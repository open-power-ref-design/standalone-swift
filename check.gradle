task check (type : Exec) {
    print 'Check task on standalone-swift:'
    workingDir '.'
    // Run the default tox environment.
    executable = "tox"
    args "-r"
}
task do_all{
  check.execute();
}
